# Python scripts convention

**All new Python scripts belong in `~/pythons`.**

- Create or move any `.py` script here (or in an appropriate subfolder under `~/pythons`).
- Run from anywhere with: `python3 ~/pythons/script_name.py`
- Shell/automation scripts can stay elsewhere but should call Python via `python3 ~/pythons/...` when they use these scripts.
